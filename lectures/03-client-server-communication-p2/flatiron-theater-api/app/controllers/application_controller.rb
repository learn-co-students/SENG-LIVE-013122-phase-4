class ApplicationController < ActionController::API

    #When a user logs in we will want to save their id for easy access
    #In week 2 we will handle this with sessions 
    #For week 1 we will hardcode current_user.

    
end
